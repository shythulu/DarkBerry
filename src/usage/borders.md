1. Copy a flavour from this folder over `~/.config/borders/bordersrc`.
2. Restart borders: `brew services restart borders`.

The focused window's border takes `ui.border.active`, which is the accent. Every other
window takes `ui.border.inactive`. Width, style and hidpi in the file are the stock
defaults. The two colours are the whole theme.
